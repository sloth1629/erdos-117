# ERDOS117_GLOBAL_PRO_RESULT

## Outcome

`[PROVED]` This bundle proves a self-contained twisted-tensor partial-ovoid theorem

\[
\pi(q,2^{t-1})\ge q^t+1
\qquad(q\text{ a prime power},\ t\ge1\text{ odd}),
\]

and derives the following consequences for the scalar symplectic groups \(S(q,m)\):

1. `[PROVED]` for every fixed \(q>2\), \(\log(q^m+1)/\pi(q,m)\to0\);
2. `[PROVED]` the same convergence is uniform over all varying prime powers \(q\ge3\) whenever \(\pi(q,m)\to\infty\);
3. `[PROVED]` the union of all nonbinary scalar models has exponential base one;
4. `[PROVED]` the full scalar-symplectic envelope has exact exponential base \(\sqrt2\), supplied only by \(q=2\);
5. `[PROVED]` except for \((q,m)=(2,1)\), one has \(q^m+1\le2^{\pi(q,m)/2}\).

`[UNVERIFIED]` The full Erdős Problem 117 remains unresolved.

`[PROVED]` The scalar result in this bundle does not determine \(h(8)\), but
the surrounding repository subsequently proved the independent exact theorem
\(h(8)=10\).  Historical packet references to two cutoff-eight residual
branches are superseded by the current `STATUS.md` and exact proof files.

## Files

- `GLOBAL_RESEARCH_REPORT.md` — outcome-first report, ten Phase-I mechanisms, selection, complete proofs, audits, dependency graph, and solve-status declaration.
- `EVIDENCE_LEDGER.md` — claim-by-claim evidence labels, dependencies, scope, and gaps.
- `NEXT_ATTACK.md` — sharply scoped higher-codomain program, smallest falsification experiment, backup upper direction, and cutoff-eight tests.
- `REPRODUCE.md` — exact verification commands and environment record.
- `scripts/` — certificate generators, independent verifiers, and arithmetic audit.
- `certificates/` — exact JSON witnesses and saved transcripts.
- `MANIFEST.sha256` — hashes for every file in the bundle except the manifest itself.

## Load-bearing status

`[PROVED]` The mathematical proof does not depend on computation, CFSG, classification tables, or an inaccessible source. The computation files are independent sanity checks only.
